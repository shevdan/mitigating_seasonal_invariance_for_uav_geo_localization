# seasonalgeo: ArcGIS Wayback / Sentinel-2 / Planet retrieval pipeline.
# For the seasonal_augmentation diffusion pipeline see seasonal_augmentation/requirements.txt
# For the Sample4Geo training framework see Sample4Geo/requirements.txt

# Core
pandas>=2.0
Pillow>=10.0
tqdm>=4.65
rich>=13.0
pyyaml>=6.0
requests>=2.28
numpy>=1.24

# Geospatial I/O (used by ArcGIS provider for GeoTIFF writing)
rasterio>=1.3
opencv-python>=4.8

# Sentinel-2 provider (optional, only if running --provider sentinel2)
earthengine-api>=1.4
geemap>=0.34

# Planet Labs provider (optional, only if running --provider planet)
planet>=2.0